#include "menu-manager/EnabledCallbacks.h"

bool EnabledCallbacks::DisconnectEnabled()
{
    // Placeholder for actual logic to determine if "Disconnect" should be enabled
    // For example, check if there is an active connection
    bool hasActiveConnection = false; // Replace with actual check

    return hasActiveConnection;
}
